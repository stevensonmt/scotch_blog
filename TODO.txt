--Need authors resource (controller, login, signup, associated views and sendgrid, admin property)
--need readers resource as well
--imgur API for image hosting?
--header and footer partials
--sidebar partial
